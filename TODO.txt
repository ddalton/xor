Vision
=======
Support CRUD automation over REST using JPA
Allows developers to quickly add J2EE persistence to their UI development. Assists with RAD.
Helps bootstrap J2EE development and business logic can slowly be exposed to users over time.

1. Very fast object retrieval from an RDBMS using views, optimized for stored procedures
2. Native JSON support. Directly interact using JSON and hence just a JS library.
3. Support for models with loops using the swizzler JS library
4. Multiple query access methods:
   1. Default - By navigation
   2. OQL 
   3. SQL
   4. Stored procedure
5. Support Excel import/export for Perf data population




NEXT TASK
=========

getOutputFromparent

setterAction.setCustomValue->setValue : 638

callInfo.readCustomValue

View meta information
=====================
Add support for providing cascade information from XML files for each entity. The entity is FQCN.
With this information we efficiently retrieve using JPAViewBranchTest#testTaskQueryableRegions.
[NOTE] Queries for cascaded loops need to be provided by user. Raise this as an error if not provided.
Once this is done, all loops need to be removed (i.e., the non-cascaded backref)

This information is needed for Persistence mechanisms that don't support cascade directly.


optimization 
- If list (unless it does not have orderby or index column, then it is a bag), the other side cannot be inverse since the index information needs to be updated
- If set/bag, the other side can be inverse for performance reasons during update. Unless there is a business logic method. see #14


1. Create a model with two parallel collections (unfortunately only cascaded collections are represented and all cascaded collections are put in the same view). Check Project.
2. Check that 2 children views are generated one for each collection. This is to prevent cartesian joins (The user can always modify this structure).
3. Check that the user entered OQL is honored.



Goals/use cases
===============

High performance loading and save, meeting needs of low latency applications and cutting through ORM overhead.
   Implement use case to support same view along 4 different approaches
   1. Reachability by traversing object model (show N+1 select problem)
   2. View using HQL or JPL
   3. Same view using SQL - Hierarchical query optimization
   4. Same view using stored procedure (Using an in-memory DB that supports it).
   Shows how we can address performance without impacting the API or business logic code.
   Ensure parameter passing from client is working well.

   Also support view with multiple views. Ensure loading and linking are separte phases.
   Test this with a combination of SQL and Stored procedure views showing the flexibility of this solution.

   Create and display benchmarks if possible.

   NOTE: Bypass ORM for Stored procedures, get the connection object and prepare a callable statement
   
   Eliminate QueryView and refactor aggregateView by entityType, and dump the QueryView information in it.
   This will allow DBA and query experts to customize it according to entityType. More details:

   1. Create a new attribute in AggregateView called "System", which has the OQL/HQL generated by the system.
      This is always overwritten by the system
   2. Version enable the queries
   3. Remove versioning from the domain model

Implement AggregateViews#generateQueries
Remove    JPAViewBranchTest#testTaskQueryableRegions.
populate OQLQuery object in AggregateView. Maybe this should only have the query.

Maybe eliminate QueryView - No since QueryView is pretty rich and we only want to store a serialization friendly form in AggregateView.





**********************[Nice to have]**************************************

Batch support -
  We will not have explicit batch support, but rather implement only inverse bi-dir handling that will behave like a batch if the toMany side is not automatically linked up.
  In other words, if the persistence needs to be done only from child knowing parent, we should not have to instantiate and populate the collection there by avoiding this overhead and improving performance.

  The existing test cases need to support this change in behavior.

  Also, clean up the test cases and have a uniform model (Maybe borrow the northwind database or the supplier/parts/orders/locations database in CJ Date)


2. Test queryOperation, that automatically attaches domain objects to the session

2.1 Test modified aggregates

2.2 Test concurrency and session and entityManager


3. Additional JSON tests (View, map)

4. Test snapshot based optimistic concurrency control. Change summary is not needed.
5. Create a model with no version attribute
   e.g., UPDATE task SET name = <new_name>, description = <new_desc> WHERE name = <snapshot_name> AND description = <snapshot_desc>

6. Implement StoredProcedureQuery
7. Paging (start and count) and order by and start based off order by column
8. Denormalized query and update???

10  support view update and stale version short circuit
    transform flag in settings
    Test both simple value and collection using filter


12 Add JUnit tests for the following:
   1. Performance - show performance using a stored procedure for a view
   2. Schema evolution - show the evolution using two versions of the same view
     A/B Mode testing
     A - View with SQL
     B - View with stored procedure (different version)
   3. JUnit test showing retrieval of a complete entity with recursive relationship

13 Support stored procedures for update (create, insert, delete and SQL)
    - Similar to how Powerbuilder DataWindow operates
    - Not very scalable to handle large number of users, but very low latency
      for small deployment

15 Create test case for an extensible Facet


3. Release XOR product


*******************
Future enhancements
*******************
 

Support Amazon DynamoDB and Google cloud datastore
-- Aggregate is implemented using the following algorithm
-- Important fields are given their own columns (Support expanding this as needed, could be expensive)
-- All other data in the aggregate is stored as compressed JSON data in a separate column
By going with this approach we keep the API simple. Queries will work only with the columns identified as important fields

The compressed size limit is affected by the provider limits
DynamoDB - 400KB
Datastore - 1MB

The solution would be to split it across different rows. But that needs to be done by the application (Can be supported in the tool later).

-- Could consider supporting this approach in an RDBMS 
-- Support machine-learning performance improvement (Add frequently accessed columns and mark the row dirty if an important column is modified)

Examples
========
http://docs.aws.amazon.com/amazondynamodb/latest/developerguide/JavaDocumentAPIBinaryTypeExample.html
http://stackoverflow.com/questions/13121062/what-is-the-exact-datastore-and-memcache-entity-size-limit-and-how-is-it-calc




Versioning
==========
The viewFiles folder will have versions folders underneath it, e.g., 

20
19
18
17

Each version folder will have only those views that have changed.
A function called regenerate will merge all views valid until a certain version to keep the versions manageable
and also to deprecate old versions.

so e.g., merge(16) will regenerate all views not present in versions 17 and higher

Additionally, types whose structure has been changed in an non-supportable way will need to be 
marked in the AggregateManager along with the minimum version and a maxium version if that type is no longer
available. If the type is not listed here then it is supported across all versions.