There is each test for Hibernate and JPA.

The first set in each set takes some time to run (a couple of seconds for simple test). 
This is because the necessary persistence configuration needs to be loaded and setup by the
persistence layer.